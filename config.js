/* ============================================================
 *  Pashudhan Kartavya — front-end config  (ADMIN edits this file)
 * ------------------------------------------------------------
 *  Point the app at your PocketBase server. Officers never see
 *  or touch this — they only get a login screen.
 *
 *  Set ONE of the following, matching how you deployed PocketBase
 *  (see pocketbase/SETUP.md):
 *
 *   • Dedicated subdomain (RECOMMENDED — admin dashboard works cleanly):
 *       window.POCKETBASE_URL = 'https://pb.yourdomain.com';
 *
 *   • Same domain, reverse-proxied under /pb:
 *       Leave this file as-is (the app defaults to <this-domain>/pb).
 * ============================================================ */

window.POCKETBASE_URL = 'https://pb.187.127.146.81.sslip.io';
